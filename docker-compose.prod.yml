# El nombre de la base de producción NO se lee de POSTGRES_DB. El .env de la raíz
# es el de desarrollo y define POSTGRES_DB=itakai_dev, así que cualquier comando
# lanzado desde el repo interpolaba ese valor aquí y apuntaba producción a la base
# de dev — sin fallar, porque itakai_dev también existe en este servidor. Se usa
# PROD_POSTGRES_DB, que el .env de dev no define, para que no puedan colisionar.
services:
  # PostgreSQL Database (Production)
  postgres:
    image: postgres:16-alpine
    container_name: itakai-db-prod
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-itakai}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${PROD_POSTGRES_DB:-itakai_prod}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
      - ./api/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-itakai} -d ${PROD_POSTGRES_DB:-itakai_prod}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - itakai-network

  # Backend API (Production) — uses host network for VPN (tun0) access to Spark
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: itakai-api-prod
    network_mode: host
    env_file:
      - ./api/.env.prod
    environment:
      NODE_ENV: production
      PORT: 3001
      NODE_OPTIONS: "--dns-result-order=ipv4first"
      # DATABASE_URL ya NO se construye aquí: sale de api/.env.prod, junto al resto
      # de secretos de producción. Al estar en `environment:` pisaba al env_file y
      # heredaba del .env de dev (usuario, contraseña y base equivocados), así que
      # un deploy podía dejar la API apuntando a otra base o sin poder autenticarse.
      # deploy.sh comprueba que api/.env.prod la define antes de tocar nada.
    volumes:
      - api_uploads_prod:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  # Frontend App Builder (Production - generates static files)
  app-builder:
    build:
      context: ./app
      dockerfile: Dockerfile
      target: builder
    container_name: itakai-app-builder
    env_file:
      - ./app/.env
    environment:
      NODE_ENV: production
      NUXT_PUBLIC_API_BASE: ${NUXT_PUBLIC_API_BASE}
    command: sh -c "pnpm generate && mkdir -p /static-output && rm -rf /static-output/* && cp -a .output/public/. /static-output/"
    volumes:
      - /home/static/itakai:/static-output
    networks:
      - itakai-network
  
  nginx:
    image: nginx:alpine
    container_name: itakai-nginx
    depends_on:
      api:
        condition: service_started
      app-builder:
        condition: service_completed_successfully
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /home/static/itakai:/usr/share/nginx/html:ro
      - ./app/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    restart: unless-stopped
    networks:
      - itakai-network

volumes:
  postgres_data_prod:
  api_uploads_prod:

networks:
  itakai-network:
    name: itakai-prod-network
